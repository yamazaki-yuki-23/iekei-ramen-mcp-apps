# 機能追加のタスク（優先度順）

発散させた 100 個は [IDEAS-100.md](IDEAS-100.md)。そこから 10 個を選び、
**UX・面白さ・画期性・インパクト**で並べた。工数は順位の理由にしていない。
各項目の末尾にある目安時間は、着手を決めたあとの段取り用。

## 並べるときに置いた軸

> **このアプリのユーザーは「店を探しに来た」のではなく「今日どこで食べるかを
> 決めに来た」。** 558 件を出しても満足は生まれない。決まったときに生まれる。

だから「絞り込み手段を増やす」系は総じて下がり、「決められる」「不安が消える」
「また開きたくなる」系が上がっている。

前提として押さえておくデータの穴（558 件中）:

| フィールド              | 埋まっている件数                        |
| ----------------------- | --------------------------------------- |
| `confidence: candidate` | 224（40% が未判定のまま一覧に出ている） |
| `taste: unknown`        | 372（67%）                              |
| `openingHours`          | 107（19%）                              |
| `phone`                 | 58                                      |
| `website`               | 42                                      |

**データが 2 割しか無いフィールドを前提にした機能は作らない。**

---

## 1. 選んだ店から会話が続く（完了）

- [x] 選択中の店を `app.updateModelContext()` でモデルに渡す
- [x] 「この店について聞く」ボタンから `app.sendMessage()` で会話に繋ぐ
- [x] 選択解除・モード切り替え・再検索のときに context を消す
- [x] カードを押すと即座に外部地図が開く挙動をやめ、選択パネルに行き先をまとめる

実装: [src/lib/shop-brief.ts](src/lib/shop-brief.ts)（モデルに渡す文面）、
[src/components/SelectedShop.tsx](src/components/SelectedShop.tsx)（選択パネル）、
選択状態は [src/mcp-app.tsx](src/mcp-app.tsx) の外側コンポーネントが持つ
（Inner は payload ごとに作り直されるため）。

**UX:** 今は地図で店を選んでも、ユーザーが「この店どう？」と聞いた瞬間に
モデルは何も知らない。ユーザーから見ると **アプリと会話が別人**に見える。
ここが繋がって初めて 2 以降の対話機能が成立するので、単体の満足度より
**土台としてのインパクト**で 1 位。

`updateModelContext` は即応答を起こさず次の発話に乗る。`sendMessage` は
即応答を起こす。選択は前者、ボタンは後者。

対象: [src/mcp-app.tsx](src/mcp-app.tsx) の `selectedId`
目安: 1〜2 時間

## 2. 券売機の前で困らせない（注文カンペ）

- [ ] 店を選ぶと「麺の硬さ・味の濃さ・脂の量」の三つ巴を表示する
- [ ] 初回 / 2 回目 / 上級者の注文セットを出す
- [ ] ライス・ほうれん草・のりの扱いを一言で添える
- [ ] 店ごとの差はデータに無いので、一般的な作法だと明示する

**UX:** 家系で一番強い不安は「店が見つからない」ではなく
**券売機と『お好みは？』**。この不安は全ユーザーに毎回発生するのに、
今のアプリは 1 文字も助けていない。データ不要で解けるのに未解決という点で、
費用対効果ではなく**満足の density** が一番高い。

他のラーメン検索アプリがやっていない領域という意味で画期性もここ。

**注意:** 店ごとの流儀の違いは OSM に無い。断定しない。

目安: 半日

## 3. 「決めきる」モード

- [ ] 条件から 3 軒まで自動で絞る
- [ ] モデルが 1 軒を理由つきで推す
- [ ] 「他の 2 軒にする」「引き直す」を 1 タップで
- [ ] 決まったら 4 の経路導線に繋ぐ

**UX:** 一覧 558 件は選択肢地獄。人は理由が付いた提案でしか決断できない。
「探す」アプリから「決める」アプリへの転換なので、体験の性格が変わる。

1 が入っていればモデルに推させる部分はそのまま乗る。

目安: 半日

## 4. 行くところまで繋ぐ（はしご・経路）

- [ ] 2〜3 軒を選んで巡回順を出す（[src/lib/geo.ts](src/lib/geo.ts) の距離計算を流用）
- [ ] 地図に順路の線を引く
- [ ] Google Maps / Apple Maps に `app.openLink()` で渡す

**UX:** 決めたあと、ユーザーは必ずアプリを出て地図アプリを開く。
そこが手作業のままだと体験が途切れる。はしごは家系好きの実際の行動でもある。

目安: 半日

## 5. 訪問スタンプと制覇率

- [ ] 行った店に印を付ける
- [ ] 県ごと・全国の制覇率を出す
- [ ] 地図上で訪問済みの色を変える
- [ ] 記録の保存先（ホストの永続化に依存する点）を先に調べる

**UX:** 1 回きりの検索ツールから、**また開く理由のあるアプリ**になる。
558 件という母数が初めて「制覇対象」という意味を持つ。

**注意:** 実行時の書き込み先が無い設計なので、保存をどこに置くかの検討が先。
保存できないなら会話内に留まることを UI で明示する。

目安: 半日〜1 日（保存先の調査を含む）

## 6. 駅・ランドマークから探す

- [ ] `geocode-place` を UI の入力欄に繋ぐ
- [ ] 直線距離ではなく「徒歩◯分」に換算して出す
- [ ] 曖昧な地名のとき候補を選ばせる

**UX:** 人は緯度経度でも県名でもなく「横浜駅」「新宿」で動く。
徒歩分に換算するのは、距離を**判断できる単位**に変える作業。

目安: 半日

## 7. `candidate` 224 件を宙ぶらりんにしない

- [ ] 「家系か聞いてみる」ボタンを candidate の店にだけ出す
- [ ] 判定根拠（店名・ブランド・OSM タグ）を context に載せる
- [ ] 答えはデータに書き戻さないことを UI に明示する

**UX:** 一覧の 40% が「家系か未判定」のまま出ている。今はユーザーが
**その曖昧さを引き取らされている**。モデルに投げられれば、その場で解消できる。

**注意:** 静的 JSON なのでモデルの答えは `shops.json` に反映されない。
その場限りの参考。

目安: 半日

## 8. 地図を実際に使える大きさにする

- [ ] `app.requestDisplayMode({ mode: "fullscreen" })` をボタンに繋ぐ
- [ ] ホストが拒否したとき（`isError`）は黙って元のまま
- [ ] 現在地ピンと同心円（500m / 1km）を出す

**UX:** インライン枠の地図は狭くて、今のままだと地図モードが飾りに近い。
同心円は「歩ける範囲かどうか」を一目にする。

目安: 半日

## 9. 東京 162 件の団子を解く

- [ ] マーカーのクラスタリング
- [ ] 地図を動かしたら「このエリアで再検索」

**UX:** 東京 162 件・神奈川 121 件が重なって、地図の情報量が実質ゼロになる
ゾーンがある。8 と合わせて地図モードが成立する。

目安: 半日

## 10. 家系の歩き方カード

- [ ] 初回起動時に「家系とは」を 3 行で出す
- [ ] 用語（直系 / 資本系 / インスパイア系）を短く説明する
- [ ] 2 の注文カンペへ繋ぐ

**UX:** 家系を知らない人がいきなり 558 件を見せられても価値が分からない。
入口の 3 行が、アプリ全体の意味を決める。

目安: 2〜3 時間

---

## 検討して外した案（抜粋）

全 100 個と、惜しかった案の落選理由は [IDEAS-100.md](IDEAS-100.md) の末尾。

- **営業中フィルタ** — `openingHours` が 107 件（19%）。掛けると 8 割消える。
- **味の推定を増やす** — OSM に味のデータは無い。モデルに推測させると家系を
  名乗る店へ一律 `rich` が返る（CLAUDE.md に記録済み）。対応表からだけ取る。
- **電話・サイトの導線** — 58 件 / 42 件しか無い。
- **自然文で絞り込む** — ホストの会話が既に自然文を捌いている。役割が重複する。
